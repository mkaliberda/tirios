generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Item {
  id       Int    @id @default(autoincrement())
  name     String
  category String
  price    Int

  @@index([name, category])
}
